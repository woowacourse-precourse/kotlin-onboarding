package onboarding

fun solution7(
    user: String,
    friends: List<List<String>>,
    visitors: List<String>
): List<String> {
    TODO("프로그램 구현")
    /* var result = mutableListOf<String>()
    var score = mutableMapOf<String, Int>()

    for (f in friends) {
        if (f[1] == user) continue
        else if (listOf(f[0],user) in friends) {
            if (f[0] in score) {
                var x = score.get(f[0])?.toInt()
                score.put(f[0],x)
            }
            else score.put(f[0],10)
        }
    }

    for (v in visitors) {
        score.put(v,score.get(v))
    } */
}
