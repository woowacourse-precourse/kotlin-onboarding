package onboarding

fun solution2(cryptogram: String): String {
    TODO("프로그램 구현")
    /*
    [기능 목록]
    1. 연속으로 중복된 문자 제거
        a) 중복이 시작된 문자와 중복된 마지막 문자의 인덱스 찾기
        b) 중복된 인덱스 범위 제거
    2. 연속으로 중복된 문자가 없을 때까지 반복
     */
}
