/*
기능 목록 :
1. 중복 제거
1-1. 파라미터로 받은 문자열의 문자들을 검사해 i번째 문자와 i+1번째 문자가 같다면 두 문자 모두 제거한다.
1-2. 1-1과정을 i와 i+1 문자가 같지 않을 때까지 반복한다.
*/

package onboarding

fun solution2(cryptogram: String): String {
    TODO("프로그램 구현")
}
