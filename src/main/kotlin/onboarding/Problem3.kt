package onboarding

fun solution3(number: Int): Int {
    var threeCount = 0
    var sixCount = 0
    var nineCount = 0

    if (isCorrect(number)) {

    } else return -1
}

fun isCorrect(number: Int): Boolean {
    val condition1 = number in 1..10000
    return condition1
}
