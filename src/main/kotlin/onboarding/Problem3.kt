package onboarding

fun solution3(number: Int): Int {
    TODO("프로그램 구현")
    var array = ListOf(number % 10, (number%100)/10, (number%1000) / 100, (number % 10000) / 1000, number / 10000)
    var answer = 0

    return answer
}
