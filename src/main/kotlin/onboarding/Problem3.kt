package onboarding

fun solution3(number: Int): Int {
    //369게임
    var clap_cnt :Int = 0 //박수 카운터

    for (i in 1..number){
        //숫자 문자열로 분리
        val num_to_str = i.toString()
        
    return clap_cnt
}
